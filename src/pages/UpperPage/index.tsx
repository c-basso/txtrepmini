import { Page } from '@/components/Page';
import { UpperForm } from './UpperForm';

export const UpperPage = () => {
    return (
        <Page back>
            <UpperForm />
        </Page>
    );
}